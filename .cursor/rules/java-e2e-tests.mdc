---
description: Java Selenide e2e conventions — etalon and generated tests in GitHub project repos
globs: projects/**/*.java,templates/tests-java/**/*.java
alwaysApply: false
---

# Java E2E tests (project repo)

Each Allure TestOps project gets its own GitHub repo:  
`https://github.com/autotests-cloud/{project-name}-{projectId}`

**Эталон (разработка):** `templates/tests-java/` — pyramid, PO, visual, header; RAG `docs/rag/`.  
**GitHub bootstrap:** trim из `templates/tests-java/` через `prepare_bootstrap_workdir()` — infra без e2e test classes.  
**RAG:** rule `rag-retrieval.mdc` — vendored `docs/rag/`.  
**Static UI (local):** `templates/vanilla-ui/` + `python -m http.server 3000`.

Local working copy per project: `projects/{repo_name}/`

## Layout in project repo

```
projects/{repo_name}/src/test/java/tests/
  TestBase.java
  LoginTests.java          # @Feature «Авторизация», несколько @Test
  RegistrationTests.java   # @Feature «Регистрация»
```

## Naming

- **Class** — по `@Feature` / области: `LoginTests`, `RegistrationTests`
- **Method** — по сценарию: `successfulAuthorizationTest`, `wrongPasswordAuthorizationTest`
- **Не дублировать** синонимы: `SignInTests` → `LoginTests`
- **Append** — новый кейс той же фичи добавляется в существующий класс

## Generated test method

- Package `tests`, class extends `TestBase`
- `@AllureId("{testOpsId}")` on `@Test`
- `@DisplayName` на классе — фича; на методе — название кейса из TestOps
- Generated: `step()` + `[data-testid=…]` (Python generator)
- Etalon PO: `templates/tests-java/.../LoginTests.java`, `pages/LoginPage.java`
- Workflow skill: `.cursor/skills/automate-manual-test/SKILL.md`

## CI

GitHub Actions: `templates/tests-java/.github/workflows/selenoid-qa-guru_github.yml` (`name: qa_guru_automator_ethalon-5267 Tests`)

```bash
./gradlew test --tests tests.LoginTests.successfulAuthorizationTest -Denv=ci
```

Allure 3 report: `https://autotests-cloud.github.io/{repo}/reports/{run_id}/awesome/index.html`  
TestOps upload: `allurectl` при `ALLURE_PROJECT_ID` + `ALLURE_TOKEN`.
